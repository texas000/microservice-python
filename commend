# Package Install
pip install -r requirements.txt

# Fast API Run
uvicorn main:app --reload

# generate the req file when you import new package 
pip freeze > requirements.txt

python -m  pipreqs.pipreqs .
